apiVersion: v1
kind: Service
metadata:
  labels:
    app: simpleapi
    application: simpleapi
  name: simpleapi
  namespace: simpleapi
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: simpleapi
  sessionAffinity: None
  type: ClusterIP
